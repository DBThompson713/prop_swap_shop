<p id="notice"><%= notice %></p>


<h1>Props for swapping!</h1>

<section class="showing-all-plants">
        <% @plants.each do |plant| %>
            <%= link_to(plant) do %>
                <div class="individual-plant-show-all">
                    <h2><%= plant.name %></h2>
                    <p><%= plant.plant_type %></p>
                    <p><%= number_to_currency(plant.price / 100.0) %></p>
                </div>
            <% end %>
        <% end %>
    </section>

<!--
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Proper name</th>
      <th>Price</th>
      <th>Description</th>
      <th>Plant type</th>
      <th>Sold</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

-->



    <!--
    <% @plants.each do |plant| %>
      <tr>
        <td><%= plant.name %></td>
        <td><%= plant.proper_name %></td>
        <td><%= plant.price %></td>
        <td><%= plant.description %></td>
        <td><%= plant.plant_type %></td>-->
       <!-- <td><%= plant.sold %></td> 
        <td><%= link_to 'Show', plant %></td>
        <!--<td><%= link_to 'Edit', edit_plant_path(plant) %></td>
        <td><%= link_to 'Destroy', plant, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %> -->
  </tbody>
</table>
  

  <div class="sign_out">
  <% if user_signed_in? %> 
  <%= link_to "logout", destroy_user_session_path, method: :delete %>
  <% else %>
  <%= link_to "Login", new_user_session_path %>
  <% end %>
</div>


<br>
<!--
<%= link_to 'New Plant', new_plant_path %>
-->
